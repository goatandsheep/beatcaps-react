{
  "id": "OutputResponse",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "faker": "random.words"
    },
    "inputs": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "items": {
        "type": "string",
        "faker": "random.uuid"
      }
    },
    "templateId": {
      "type": "string",
      "faker": "random.uuid"
    }
  },
  "required": [
    "inputs",
    "templateId"
  ]
}